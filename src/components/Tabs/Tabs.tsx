// Placeholder Tabs component to avoid import errors
import { ParentComponent } from "solid-js";
export const Tabs: ParentComponent = (props) => {
  return <div>{props.children}</div>;
};

export default Tabs;
